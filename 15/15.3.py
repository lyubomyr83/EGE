P = list(range(15, 41))
Q = list(range(21, 64))
A = []  # Сюда соберем все x, которые ОБЯЗАНЫ быть в A

for x in range(1, 1000):
    # Нам нужно, чтобы выражение было истинным (1) всегда.
    # Проверим, при каких x выражение становится ЛОЖНЫМ, если x НЕ в A.
    # Если (x in A) = False делает всё выражение ложным, значит x ДОЛЖЕН быть в A.

    if not ((x in P) <= (((x in Q) and (True)) <= (not (x in P)))):
        A.append(x)

# Вычисляем длину: максимальный x минус минимальный x
print(A)
if A:
    print(f"Минимальный отрезок A: [{min(A)}; {max(A)}]")
    print(f"Минимальная длина: {max(A) - min(A)}")
else:
    print("Длина: 0")

# Ответ 19